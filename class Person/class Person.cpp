#include <iostream>
#include <string>
#include <vector>
#include "Person.h"

using namespace std;


int main()
{
    // double TOS;int NOF;
    // int *nof_ptr = &NOF;
    // sd.add_soldier(Weapon::machine_gun,TOS,NOF,Service::contract_service,Rank::soldier);
    // sd.add_soldier(Weapon::machine,TOS,NOF,Service::contract_service,Rank::soldier);
    // sd.del(0);
    // sd.del(1);
    // sd.fight();
    // sd.print();
    Person_System sd;
    sd.menu();
    return 0;
}
